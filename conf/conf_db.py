import os

# Table Name
TABEL_BROSUR = "brosur"

# Fetch the service account key JSON file contents
CRED_FIREBAE = 'firebase-adminsdk.json'
# Initialize the app with a service account, granting admin privileges
DATABASE_URL = 'https://kas-remaja.firebaseio.com/'

# Local DB
DB_NAME = "brosur.db"
